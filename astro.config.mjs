// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://tarunkavi.github.io/', // Replace with your site URL
    base: '/tarunkavi', // Replace with your repo name
});
